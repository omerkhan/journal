---
const currentYear = new Date().getFullYear();
---

<footer class="py-12 px-4 sm:px-6 lg:px-8 mt-auto border-t border-border">
  <div class="max-w-2xl mx-auto">
    <div class="mb-8">
      <h3 class="font-serif text-lg text-text-primary mb-2">Subscribe</h3>
      <p class="text-text-secondary text-sm mb-4">Get occasional updates when I post something new.</p>
      <form
        action="https://buttondown.com/api/emails/embed-subscribe/omerkhan"
        method="post"
        target="popupwindow"
        class="flex gap-2 max-w-md"
      >
        <input
          type="email"
          name="email"
          placeholder="your@email.com"
          required
          class="flex-1 px-4 py-2 text-sm border border-border rounded-lg bg-surface text-text-primary placeholder:text-text-secondary focus:outline-none focus:border-accent"
        />
        <button
          type="submit"
          class="px-4 py-2 text-sm font-medium bg-accent text-white rounded-lg hover:bg-accent-hover transition-colors duration-200"
        >
          Subscribe
        </button>
      </form>
    </div>
    <p class="text-text-secondary text-sm">
      &copy; {currentYear} Omer Khan
    </p>
  </div>
</footer>
